/*
 *
 * {{ properCase name }} actions
 *
 */

import {
{{ constantCase name }}_DEFAULT_ACTION,
} from './constants';

export function defaultAction() {
  return {
    type: {{ constantCase name }}_DEFAULT_ACTION,
  };
}
